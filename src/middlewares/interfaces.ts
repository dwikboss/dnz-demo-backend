export interface ExtractedProduct {
    keywords: string[];
    amount: number;
}

export interface CatalogProduct {
    name: string;
    description: string;
    price: number;
    sku: string;
    amount: number;
}

